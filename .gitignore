# Personal GitHub billing / AI-usage exports — never commit (may contain private usage data)
reconcile.csv
ai-usage.csv
reconcile/

# Python + build caches
__pycache__/
.cache/
.pytest_cache/
out/

# Extension build artifacts
node_modules/
extension/out/
*.vsix

# Bundled Python copies — build output of extension/scripts/bundle.js.
# Regenerated by `npm run bundle` / `vscode:prepublish`; never hand-edit.
extension/py/usage.py
extension/py/dashboard_template.py
extension/py/ghcp/
